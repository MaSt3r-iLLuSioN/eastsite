{% macro adminNavbarMacro(menuItem) %}
    {% import _self as self %}
    {% for child in menuItem.children %}
        <a class="dropdown-item" href="{{child.url}}">{{child.title}}</a>
    {% endfor %}
{% endmacro %}