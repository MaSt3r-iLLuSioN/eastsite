{% extends "base.html.twig" %}
{% block title %}Contact Us{% endblock %}
{% block page_header %}Contact Us{% endblock %}
{% block body %}
    <div class="col-md-8 contact-form contact-info-section">
        <!--
        <form class="shake" action="{{ path('contact') }}" method="post">
            <div class="form-group">
                <label for="name" class="sr-only">Name</label>
                {{form_widget(form.name)}}
                <div class="help-block with-errors">
                    {% if form_errors(form.name) %}
                        <ul class="list-unstyled"><li>Please enter your name</li></ul>
                    {% endif %}
                </div>
            </div>
            <div class="form-group">
                <label for="email" class="sr-only">Email</label>
                {{form_widget(form.email)}}
                <div class="help-block with-errors">
                    {% if form_errors(form.email) %}
                        <ul class="list-unstyled"><li>Please enter your Email</li></ul>
                    {% endif %}
                </div>
            </div>
            <div class="form-group">
                <label for="subject" class="sr-only">Subject</label>
                {{form_widget(form.subject)}}
                <div class="help-block with-errors">
                    {% if form_errors(form.subject) %}
                        <ul class="list-unstyled"><li>Please enter your message subject</li></ul>
                    {% endif %}
                </div>
            </div>
            <div class="form-group">
                <label for="message" class="sr-only">Message</label>
                {{ form_widget(form.message)}}
                <div class="help-block with-errors">
                    {% if form_errors(form.message) %}
                        <ul class="list-unstyled"><li>Write your message</li></ul>
                    {% endif %}
                </div>
            </div>
            <button class="btn btn-common btn-md" type="submit" id="form-submit" style="pointer-events: all; cursor: pointer;"><i class="fa fa-save"></i> Send Message</button>
        </form>
        -->
        <!-- Form -->

        <script type="text/javascript" src="https://buildertrend.net/leads/contactforms/js/btClientContactForm.js"></script>

        <iframe src="https://buildertrend.net/leads/contactforms/ContactFormFrame.aspx?builderID=19608" scrolling="no" id="btIframe" style="background:transparent;border:0px;margin:0 auto;width:100%;"></iframe>

        <!-- /Form -->
    </div>
{% endblock %}